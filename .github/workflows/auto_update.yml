on:
 schedule:
   - cron:  "20,50 0-14,23 * * *"
 workflow_dispatch:
 watch:
    types: [started]

jobs:
  repo-sync:
    runs-on: ubuntu-latest
    steps:
    - name: repo-sync
      run: |
        latest_version=`wget -O- https://pkgs.tailscale.com/stable/ | grep tailscale_ | head -1 | cut -d'_' -f 2`
        curl -sL -w %{http_code} https://github.com/${{ github.repository }}/archive/refs/tags/$latest_version.zip
        result=$(echo $http_code | grep "200")
        echo $result

        # if [[ "$(test_connect $1)" -eq "200" ]]; then
        
        # wget https://github.com/${{ github.repository }}/archive/refs/tags/${{ env.latest_version }}.zip 
        
        
        ls
